---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "alphadoiy",
      url: "https://yangwx.xyz/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "alphadoiy",
      logo: {
        "@type": "ImageObject",
        url: "https://yangwx.xyz/avatar.jpeg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://yangwx.xyz/avatar.jpeg",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "alphadoiy",
    url: "https://yangwx.xyz",
    image: "https://yangwx.xyz/avatar.jpeg",
    sameAs: [
      "https://github.com/alphadoiy",
      "https://x.com/3kxdUtEa4ZNfe5B",
    ],
    jobTitle: "Software Engineer",
    description:
      "武汉程序员萌新，热爱编程与开源，记录成长点滴。",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "alphadoiy",
    url: "https://yangwx.xyz",
    description:
      "武汉程序员萌新，热爱编程与开源，记录成长点滴。",
    author: {
      "@type": "Person",
      name: "alphadoiy",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://yangwx.xyz/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />